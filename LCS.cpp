#include<bits/stdc++.h>
using namespace std;

void LCS(string X, string Y){
    int m=X.length();
    int n=Y.length();
    vector<vector<int>> b();
    vector<vector<int>> c;
}

int main(){
    string X, Y;
    cout<<"Enter X"<<endl;
    cin>>X;
    cout<<"Enter Y"<<endl;
    cin>>Y;
    LCS(X, Y);
    return 0;
}